name: Self-Hosted Runner Hello World

on:
  push:
    branches:
      - branch1

jobs:
  hello-hem:
    runs-on: self-hosted

    steps:
        - name: Display Hello World
        run: echo "Hello, World!" 
